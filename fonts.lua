local fonts = {}

fonts.mainmenu = love.graphics.newFont(20)

fonts.fpsmeter = love.graphics.newFont(16)

fonts.tutorial = love.graphics.newFont(18)

fonts.timer = love.graphics.newFont(12)

fonts.title = love.graphics.newFont(48)

fonts.credits = love.graphics.newFont(12)

return fonts